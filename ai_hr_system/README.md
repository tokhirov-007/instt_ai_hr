# Система AI HR - Анализ Резюме и Собеседований

Этот проект представляет собой автоматизированную HR-систему на базе ИИ, которая анализирует резюме, проводит автоматические интервью и предоставляет рекомендации по найму на основе технических навыков и поведения кандидата.

## 🚀 Возможности

- **Умный анализ CV**: Извлечение навыков из PDF/DOCX с использованием NLP (SpaCy) и семантических эмбеддингов.
- **Мультиязычность**: Поддержка английского, русского и узбекского языков. Нормализация кириллических навыков в канонические английские названия.
- **Автоматизированное интервью**: Генерация технических вопросов на основе уровня и навыков кандидата.
- **Строгая система оценки**: Штрафы за «мусорные» ответы и обнаружение контента, созданного ИИ, или плагиата.
- **Панель администратора**: Современный интерфейс для HR для отслеживания кандидатов, просмотра отчетов ИИ и принятия решений.
- **Telegram бот**: Уведомления для HR в реальном времени с кнопками быстрого действия (Пригласить/Отклонить).

---

## 🛠 Установка

### 1. Требования
- Python 3.9+
- Pip

### 2. Установка зависимостей
```bash
pip install -r requirements.txt
```

### 3. Настройка NLP моделей
Система автоматически скачает модели при первом запуске, но вы можете установить их заранее:
```bash
python -m spacy download en_core_web_sm
```

### 4. Конфигурация
# SMTP для уведомлений по почте
SMTP_SERVER=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=your_email@gmail.com
SMTP_PASSWORD=your_app_password

# Сетевые настройки
ALLOWED_ORIGINS=*
BACKEND_URL=http://localhost:8000
```

---

## 🏃 Запуск системы

### 1. Запуск API сервера
Запустите основной бэкенд (FastAPI):
```bash
python -m app.main
```
- **Приложение для кандидата**: `http://localhost:8000`
- **Панель администратора**: `http://localhost:8000/admin-ui`
- **Документация API (Swagger)**: `http://localhost:8000/docs`

### 2. Запуск Telegram бота
В отдельном терминале запустите обработчик бота:
```bash
python -m app.bot.bot
```

---

## 🌐 Доступ по локальной сети (с других ПК)

Чтобы открыть админку или приложение с другого компьютера в той же Wi-Fi сети:

1. **Найдите ваш локальный IP**:
   - Windows: `ipconfig` (ищите IPv4 адрес, например, `192.168.1.10`)
   - Linux/Mac: `hostname -I`

2. **Доступ через IP**:
   - Откройте `http://ВАШ_IP:8000/admin-ui` в браузере на другом ПК.

3. **Обновление конфига бота**:
   Если бот запущен в другой среде, установите `BACKEND_URL` в `.env` на `http://ВАШ_IP:8000`.

---

## 📂 Структура проекта

- `app/cv_intelligence`: Логика парсинга CV и извлечения навыков.
- `app/scoring`: Логика оценки ответов и генерации рекомендаций.
- `app/bot`: Реализация и обработчики Telegram бота.
- `static/`: Фронтенд (HTML, CSS, JS) для кандидата и админа.
- `uploads/`: Директория для хранения загруженных резюме.
- `hr_system.db`: База данных SQLite для сессий кандидатов.

---

## 🧪 Проверка (Тесты)
Вы можете запустить скрипты проверки, чтобы убедиться, что всё работает:
```bash
python verify_resume_and_scoring.py  # Тест логики оценки и CV
python verify_loose_validation.py    # Тест строгости загрузки CV
```
